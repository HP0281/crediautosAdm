import { NgDominicodeFilesDirective } from './ng-dominicode-files.directive';

describe('NgDominicodeFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDominicodeFilesDirective();
    expect(directive).toBeTruthy();
  });
});
