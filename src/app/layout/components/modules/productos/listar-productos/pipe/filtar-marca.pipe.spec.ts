import { FiltarMarcaPipe } from './filtar-marca.pipe';

describe('FiltarMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltarMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
